#include <unistd.h>
#include <stdio.h>

void ft_search_replace(char *str, char search, char replace)
{

}

int main(int argc, char **argv)
{
    int i = 0;
    if (argc != 4)
        write (1, "\n", 1);
    while (argv[i])
    {
        if 
        i++;
    }
}