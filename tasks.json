{
    "tasks": [
        {
            "type": "shell",
            "label": "Build ft_printf",
            "command": "/usr/bin/make",
            "args": [
				"-C",
                "${workspaceFolder}/", // Change to the directory of your Makefile
                "all" // The target to build, default is 'all'

			],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
