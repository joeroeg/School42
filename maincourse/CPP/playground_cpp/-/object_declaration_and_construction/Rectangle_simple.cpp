#include "Rectangle_simple.hpp"

Rectangle_simple::Rectangle_simple() : width(0), height(0) {
    // Additional initialization logic if needed
}

Rectangle_simple::Rectangle_simple(int w, int h) : width(w), height(h) {
    // Additional initialization logic if needed
}
